@extends('layouts.web')
@section('content')
<style>.reports{padding:2rem 0 4rem}.reports h1{font-size:2.6rem;margin:.3rem 0}.subnav{display:flex;gap:1rem;flex-wrap:wrap;margin:1rem 0 2rem}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:1rem}.table{width:100%;border-collapse:collapse}.table th,.table td{padding:.7rem;border-bottom:1px solid var(--line);text-align:left;vertical-align:top}.inline{display:inline-flex;gap:.4rem;align-items:center}.field select,.field textarea{width:100%;padding:.75rem;border:1px solid #b9c5d3;border-radius:.55rem}.small{font-size:.82rem}.pill{display:inline-block;padding:.2rem .55rem;border-radius:1rem;background:#eef5ff}.danger{background:var(--danger)}@media(max-width:800px){.grid{grid-template-columns:1fr}.table{display:block;overflow:auto}}</style>
<div class="wrap reports"><div class="eyebrow">Academic reporting</div><nav class="subnav"><a href="{{ route('reports.dashboard') }}">Dashboard</a><a href="{{ route('reports.index') }}">Report directory</a><a href="{{ route('reports.generate.form') }}">Generate</a><a href="{{ route('reports.scales') }}">Grading scales</a><a href="{{ route('reports.periods') }}">Periods</a><a href="{{ route('reports.templates') }}">Templates</a></nav>@if(session('status'))<div class="panel">{{ session('status') }}</div>@endif @if($errors->any())<div class="errors"><ul>@foreach($errors->all() as $e)<li>{{ $e }}</li>@endforeach</ul></div>@endif @yield('reports-content')</div>
@endsection
