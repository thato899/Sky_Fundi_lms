@extends('reports.layout')
@section('title','Grading scales')
@section('reports-content')<h1>Grading scales</h1><p>Bands use inclusive two-decimal boundaries and must continuously cover 0.00–100.00 without overlap.</p>@if(in_array('reports.manage_grading_scales',$permissions,true))<form method="POST" action="{{ route('reports.scales.store') }}" class="panel">@csrf<div class="field"><label>Name<input name="name" required></label></div><div class="field"><label>Code<input name="code"></label></div><div class="field"><label>Pass threshold<input name="pass_threshold" type="number" min="0" max="100" step=".01"></label></div><input type="hidden" name="is_active" value="1"><h3>Bands</h3>@foreach([['Needs support',0,49.99,'N'],['Achieved',50,74.99,'A'],['Excellent',75,100,'E']] as $i=>$b)<div class="inline"><input name="bands[{{$i}}][label]" value="{{$b[0]}}" required><input name="bands[{{$i}}][minimum_percentage]" value="{{$b[1]}}" type="number" step=".01" required><input name="bands[{{$i}}][maximum_percentage]" value="{{$b[2]}}" type="number" step=".01" required><input name="bands[{{$i}}][symbol]" value="{{$b[3]}}"></div>@endforeach<button type="submit">Create scale</button></form>@endif<table class="table"><thead><tr><th>Name</th><th>Bands</th><th>State</th><th>Actions</th></tr></thead><tbody>@foreach($scales as $s)<tr><td>{{$s->name}}</td><td>@foreach($s->bands as $b)<div>{{$b->label}}: {{$b->minimum_percentage}}–{{$b->maximum_percentage}} {{$b->symbol}}</div>@endforeach</td><td>{{$s->is_active?'Active':'Inactive'}} {{$s->is_default?'· Default':''}}</td><td>@if(in_array('reports.manage_grading_scales',$permissions,true))@foreach([$s->is_active?'deactivate':'activate','default'] as $a)<form class="inline" method="POST" action="{{ route('reports.scales.'.$a,$s->uuid) }}">@csrf<button>{{$a}}</button></form>@endforeach@endif</td></tr>@endforeach</tbody></table>{{$scales->links()}}@endsection
