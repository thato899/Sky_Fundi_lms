@extends('layouts.web')
@section('title', 'Log in')
@section('content')
<section class="form-shell panel"><div class="eyebrow">Secure application access</div><h1 style="font-size:2.2rem">Welcome back</h1><p>Use the account provided by your platform or organization administrator.</p>@if($errors->any())<div class="errors" role="alert">We could not sign you in. Check your details and try again.</div>@endif<form method="POST" action="{{ route('login.store') }}">@csrf<div class="field"><label for="email">Email address</label><input id="email" name="email" type="email" autocomplete="email" value="{{ old('email') }}" required autofocus aria-describedby="email-error">@error('email')<div id="email-error" class="error">{{ $message }}</div>@enderror</div><div class="field"><label for="password">Password</label><input id="password" name="password" type="password" autocomplete="current-password" required>@error('password')<div class="error">{{ $message }}</div>@enderror</div><label class="check"><input name="remember" type="checkbox" value="1" @checked(old('remember'))> Keep me signed in on this device</label><div class="actions"><button type="submit">Log in</button></div></form></section>
@endsection
