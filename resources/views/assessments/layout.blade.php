@extends('layouts.web')
@section('content')
<style>.assessment-shell{padding:2rem 0 4rem}.bar,.actions{display:flex;justify-content:space-between;align-items:center;gap:.8rem;flex-wrap:wrap}.metric-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:1rem}.metric{background:var(--card);border:1px solid var(--line);border-radius:.8rem;padding:1rem}.metric strong{display:block;font-size:1.5rem}.filters,.academic-form{display:grid;grid-template-columns:repeat(3,1fr);gap:.8rem;margin:1rem 0}.filters input,.filters select,.academic-form input,.academic-form select,.academic-form textarea,.table input,.table select{width:100%;padding:.65rem;border:1px solid var(--line);border-radius:.5rem}.wide{grid-column:1/-1}.table{width:100%;border-collapse:collapse}.table th,.table td{padding:.7rem;border-bottom:1px solid var(--line);text-align:left;vertical-align:top}.table-wrap{overflow:auto}.muted{color:var(--muted)}@media(max-width:800px){.metric-grid,.filters,.academic-form{grid-template-columns:1fr 1fr}}@media(max-width:520px){.metric-grid,.filters,.academic-form{grid-template-columns:1fr}}</style>
<div class="wrap assessment-shell"><nav class="bar"><div><div class="eyebrow">{{ $organization->name }}</div><a href="{{ route('dashboard') }}">Dashboard</a> / <a href="{{ route('assessments.index') }}">Assessments</a></div><div class="actions">@if(in_array('assessments.create',$permissions,true))<a class="button" href="{{ route('assessments.create') }}">Create assessment</a>@endif <a class="button secondary" href="{{ route('assessments.gradebook') }}">Gradebook</a> @if(in_array('assessments.view_reports',$permissions,true))<a class="button secondary" href="{{ route('assessments.reports') }}">Reports</a>@endif @if(in_array('assessment_categories.manage',$permissions,true))<a class="button secondary" href="{{ route('assessment-categories.index') }}">Categories</a>@endif</div></nav>
@if(session('status'))<p class="status-message">{{ session('status') }}</p>@endif @if($errors->any())<div class="errors" role="alert"><ul>@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif @yield('assessment-content')</div>
@endsection
