@extends('layouts.web')
@section('title','My attendance')
@section('content')<div class="wrap" style="padding:2rem 0"><div class="eyebrow">Learner portal</div><h1>My attendance</h1><p class="meta">Only finalized attendance is shown here.</p><div class="metric-grid">@foreach(['present'=>'Present','absent'=>'Absent','late'=>'Late','excused'=>'Excused'] as $key=>$label)<div class="metric"><span>{{ $label }}</span><strong>{{ $totals[$key] ?? 0 }}</strong></div>@endforeach</div><div class="panel"><h2>Attendance history</h2>@forelse($entries as $entry)<p><strong>{{ $entry->session?->session_date?->toDateString() }}</strong> · {{ ucfirst($entry->status->value) }}<br><span class="meta">{{ $entry->session?->classGroup?->name }}@if($entry->session?->subject) · {{ $entry->session->subject->name }}@endif</span></p>@empty<p class="empty">No finalized attendance records are available yet.</p>@endforelse</div>{{ $entries->links() }}</div>@endsection
