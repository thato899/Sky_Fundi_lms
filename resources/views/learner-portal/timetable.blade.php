@extends('layouts.web')
@section('title','My timetable')
@section('content')<div class="wrap" style="padding:2rem 0"><div class="eyebrow">Learner portal</div><h1>My next 7 days</h1><p class="meta">Your scheduled lessons for the coming week.</p><div class="panel">@if($learner->current_class_id===null)<p class="empty">Your class placement has not been confirmed yet. Please contact your school.</p>@elseif($lessons->isEmpty())<p class="empty">No lessons are scheduled for your class in the next seven days.</p>@else @foreach($lessons as $lesson)<article style="padding:1rem 0;border-bottom:1px solid var(--line)"><strong>{{ $lesson->lesson_date->toDateString() }} · {{ $lesson->subject?->name }}</strong><br><span class="meta">{{ $lesson->starts_at?->format('H:i') }}–{{ $lesson->ends_at?->format('H:i') }}@if($lesson->room) · {{ $lesson->room->name }}@endif</span></article>@endforeach @endif</div></div>@endsection
