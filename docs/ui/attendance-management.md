# Attendance web interface

`/attendance` provides organization filters, factual finalized totals, session creation and responsive registers. “Mark all present” only updates the unsaved browser form. Finalized registers are read-only. `/learners/{learner}/attendance` shows finalized recorded history.
