# Plan — Sky Fundi Platform

_Updated: 2026-08-21. **Paused mid-"Finish the Platform" build plan — see "Next steps on resume" below, that section is current.** Everything from here down to "Working method" is preserved for history and is stale relative to `main`._

## Next steps on resume (2026-08-21)

Working from the user's "Finish the Platform" 5-phase build prompt (billing → learner invitations → real Gemini/AI tutor → admin surfaces → hardening). **Phase 1 (real billing) is implemented, fully verified, and paused — not committed, not pushed, not merged.**

- **Branch:** `feature/billing/payment-gateway-integration`, cut from `main` at `179590a`. Uncommitted working tree.
- **What's done:** `core/Billing` (PayFast gateway per ADR-010, checkout, webhook-verified payments, usage-linked invoicing from Attendance data), a real `plans` table replacing `config('hackathon.plans')`, org-admin `/billing` page, Guardian Portal invoices page, `BillingPermissionSeeder` (fixed a real gap — `core.billing.manage` was platform-only, not reachable by an org admin, before this). Full detail in `core/Billing/README.md` and `docs/adr/010-payfast-payment-gateway.md`.
- **Verified canonically via Docker** (`docker compose up -d`, with a local untracked `compose.override.yaml` remapping MySQL's host port 3307→3308 to dodge an unrelated container on this machine): `composer validate --strict` ✅, `make migrate-check` ✅ (real MySQL forward/seed/rollback/re-migrate), `php artisan test` ✅ **293/1594**, Pint ✅ clean on every changed file, PHPStan — 10 findings, all proven pre-existing (reproduces identically against untouched `core/Subscriptions/Application/SubscriptionService.php`; plain PHPStan without Larastan can't resolve Eloquent's `Model::create()`/magic property access anywhere in this codebase).
- **Before Phase 1 can be called fully done:**
  1. Get PayFast sandbox merchant credentials from the user → run one real webhook-verified sandbox payment (everything is built and unit/feature-tested against PayFast's documented request/response shapes, but never round-tripped against the real sandbox).
  2. User decides when to commit/push/open a PR — not authorized yet.
  3. Decide what to do with `compose.override.yaml` (currently untracked, harmless): keep it, add to `.gitignore`, or delete. It's not part of the Billing feature.
- **Phase 2 (learner invitations/onboarding) — turned out already implemented, just untested; now tested.** `LearnerInvitationService`/`LearnerInvitationController` (API+web)/routes/views/permissions already existed in full, mirroring `GuardianInvitationService` field-for-field (hashed 7-day tokens, queued email, new/existing-account acceptance, membership activation) — `status.md`'s "next task" note predated this being built, on `main` before this session, undocumented. Found it had **zero test coverage**. Added `modules/Learners/tests/Feature/LearnerInvitationOnboardingTest.php` (8 tests, mirroring `GuardianInvitationOnboardingTest`'s pattern: happy-path invite, resend/rotate, revoke/expired/reused tokens, new-account acceptance + portal access enablement, existing-account acceptance, own-portal reachability + no-cross-org-membership isolation, cross-org token-resolution isolation, invitable-status guard). Also closed a real UI gap: the learner profile's web page only showed the "send invitation" form before any invite existed and hid it forever after (since it gated on `portal_access_enabled`, which only flips on acceptance) — a sent-but-not-yet-accepted invitation had no resend/revoke UI at all. Fixed to show status/expiry/resend/revoke once an invitation exists, mirroring the guardian profile page. Updated `modules/Learners/README.md` and `docs/roadmap.md` (moved from "Planned" to "Completed") to stop misdescribing this as unbuilt. Verified: full suite **301 tests / 1628 assertions OK**, Pint clean, `git diff --check` clean. Still on the same uncommitted branch as Phase 1 — nothing committed/pushed.
- **Phase 3, slice 1 (real Gemini provider) — done.** `core/AIGateway/Infrastructure/Providers/GeminiProvider.php` now implements `AIProviderInterface` for real against Google's Generative Language API (`{base_url}/models/{model}:generateContent`, `:streamGenerateContent?alt=sse`), auth via the `x-goog-api-key` header (not a query-string key, so it never lands in access logs). Structured output uses `responseMimeType: application/json` + a schema-describing system instruction (DeepSeekProvider's proven strategy) rather than Gemini's native `responseSchema`, which uses its own upper-case type vocabulary a translator could get subtly wrong on nested schemas — documented as a deliberate choice in the class docblock. Disabled by default (`AI_GEMINI_ENABLED=false`) pending a real `AI_GEMINI_API_KEY`; nothing routes to it until both are set. Updated `core/AIGateway/README.md`, `ProviderNotAvailableException`'s docblock, and `status.md` to stop calling Gemini (and, while already there, the already-live OpenAI) a placeholder — only Claude still is. New `tests/Unit/AIGateway/GeminiProviderTest.php` (17 tests) mirrors `DeepSeekProviderTest`'s coverage: plain + structured completion, markdown-fence stripping, invalid/prose-wrapped JSON rejection, safety-block and empty-candidate handling (Gemini-specific), streaming success/failure/malformed-event, unavailable-provider short-circuit. Verified: full suite **318 tests / 1660 assertions OK**, Pint clean, PHPStan clean, `git diff --check` clean. Same uncommitted branch, nothing pushed.
- **Phase 3, rest of it — done.** ADR-011 (`docs/adr/011-materials-retrieval.md`) decides a new `modules/Materials` module (not an Assessments extension — the retrieval pipeline serves two independent consumers, Assessments' quiz drafting and the learner tutor chat) and the concrete design: text-only ingestion (PDF deferred — no extraction library in `composer.json`, and adding one is a real dependency decision this phase doesn't make unilaterally), an async `ChunkAndEmbedMaterialJob` (paragraph-aware ~800-char chunks, `core.ai` queue), and brute-force cosine similarity in PHP over an organization's JSON-stored embedding vectors (no vector DB — MySQL 8.0/SQLite here have neither). `core/AIGateway` gained a narrow `Contracts\EmbeddingProviderInterface` + `AIManager::embed()`, implemented only by `GeminiProvider` (Google's `embedContent`, a separate model from completions). `Application\TutorChatService` (`POST /api/v1/materials/tutor-chat`) resolves the learner's organization from their own record exactly like `LearnerPortalController` — never client input — so retrieval is always organization-scoped at the query level; `tests/Feature/TutorChatIsolationTest.php` proves a learner in one org is never answered from another org's material (the brief's explicit hard requirement). `Modules\Assessments\Application\QuizDraftService` (new documented Assessments→Materials dependency, `module.json` bumped to 1.4.0) retrieves + asks Gemini for structured draft questions, then inserts every one through the *existing* `QuizService::addQuestion()` — same Draft-status/ownership gate as a hand-typed question, same `publish()` requirement before any learner sees it; a malformed AI question is skipped, not fatal. New web UI: a "Generate AI draft questions" form on the quiz show page, gated on a new `quizzes.ai_draft` permission. New tests: `modules/Materials/tests/Feature/{MaterialIngestionTest,TutorChatIsolationTest}.php` and `modules/Assessments/tests/Feature/QuizDraftServiceTest.php`.
- **Verified canonically end-to-end via Docker**, including the Materials migrations: `composer validate --strict` ✅, `make migrate-check` ✅ (real MySQL forward/seed/rollback/re-migrate), `docker compose exec app php artisan test` ✅ **330 tests / 1696 assertions, real MySQL**, Pint ✅ clean, PHPStan — clean on every changed production file (a handful of the same proven-pre-existing test-file findings remain, see the Phase 1 note above for the isolated proof this class is not something this work introduced). Along the way, fixed the container image staleness that was crash-looping `queue`/`scheduler` (`docker compose build` had never been re-run for them in weeks; `docker compose build && up -d --force-recreate` fixed it) and ran `composer dump-autoload` for the new `Materials`/`Database\Seeders` namespace mappings — both are environment fixes, not code changes, and both are now in a healthy state.
- **Then Phase 4** (enrolment-history/teaching-assignment admin surfaces) **and Phase 5** (Attendance/Reports/Scheduling/Staff test-depth parity, remaining placeholder Core adapters, deployment automation + an actually-executed restore drill, enforced 2FA).
- **Noticed but out of scope, worth a look sometime:** a stray `core/Api/Exceptions/Untitled-1.py` (unrelated Python practice snippet, not touched); repo-wide CRLF/Pint drift across ~26 files nobody's touched recently (`git diff --stat` on them is empty against `main`, so it's pre-existing, not a regression); the `app` container's `/up` healthcheck throws an `EncryptionServiceProvider` exception unrelated to Billing — worth a look if it's not already known.

## Tonight's hackathon sprint (in priority order, per product direction)

**Goal:** a running, polished, demo-ready LMS showing the core user workflows — learner and teacher first, then guardian — plus a finances/profitability view.

1. **LMS runs.** Docker stack up (`init` completed; app/queue/scheduler/Mailpit/MySQL), database migrated and seeded, `HackathonDemoSeeder` loaded, all demo-path pages return 200. This is the gate for everything else.
2. **Learner workflow.** Login → see assigned quiz → take quiz → deterministic + AI-suggested marks → released results → adaptive study plan. Smooth, obvious, no dead ends.
3. **Teacher workflow.** Login → create/populate assessment/quiz → capture or approve AI-suggested marks → release → intervention/risk dashboard.
4. **Guardian workflow.** Invited guardian → portal → linked learner's progress (restricted view).
5. **UI/UX overhaul.** The single Blade layout and the demo-path screens (entry, login, dashboards, quiz flow, subscription) get a coherent, modern visual identity: navigation, typography, color system, cards, empty states. Server-rendered CSS only — no build pipeline.
6. **Finances/profitability.** Extend the `/subscription` hackathon billing surface into a clear profitability story: revenue per seat vs AI/infra cost, margin per organization.
7. **Final verification and push.** Targeted tests + full suite, commit, push each finished chunk.

**Demo environment notes (Windows host):** shell scripts must be LF (fixed locally via `core.autocrlf input`); the baked Docker image collapses `Database/` vs `database/` module folders on Windows checkouts — use the bind-mounted dev stack (unaffected) for the demo; a standalone `sf_test` container (image + symlinks) is the fast test rig.

## Shipped today

- **Phase 0** — status/plan docs + CI-claim correction: PR #36 (draft).
- **Phase 1 — historical enrolment**: `learner_enrolments` history table, backfill, transactional timeline maintenance in `LearnerService`, self-healing for pre-tracking learners, enrolment-aware report-card calculation, ADR-008: PR #37 (draft). Full suite 267 tests / 1488 assertions green; Pint and PHPStan clean on changed paths; MySQL forward migration verified in the dev stack.
- **Hackathon sprint items 1–6 — done**: PR #38 (stacked on #37). LMS running and seeded on :8001; persona-aware routing + navigation (learner/teacher/guardian/admin all land correctly, verified live); UI overhaul of layout, quiz attempt/result, teacher review, guardian portal; subscription page reframed as profitability (validated bar palette). Remaining before the demo: merge PRs #37 → #38 (human or explicit authorization), rehearse the 5–7 minute script in `docs/hackathon-demo.md`, and optionally set an OpenAI key for live AI marking (falls back to seeded results without it).

## Immediate housekeeping (2026-07-21)

- **Infra-fix PR #47 (open):** three fixes. (1) `.dockerignore` now excludes the dangling `public/storage` symlink (Docker builds were failing with `invalid file request public/storage` on Windows hosts). (2) `docker/init.sh`'s APP_KEY guard is corrected from BRE `.+` (literal `+`) to `..*` — the old guard regenerated APP_KEY on every init rerun, logging out all sessions and orphaning encrypted values. (3) The Learners/Assessments provider migration-path logic from `8ef5ac2` is corrected — it preferred `Database/` whenever present, which on case-sensitive checkouts (CI) loaded no migrations and turned `main`'s CI red; the fix tests for the migrations directory itself. Verified against a full stack rebuild and a case-sensitive layout in the sf_test rig.

## Post-hackathon roadmap (now the active track)

1. ~~**Teacher/class/subject assignments**~~ **Done — implemented in PR #41** (ADR-009 accepted): schema, `TeachingAssignmentService`, opt-in enforcement across Assessments/Attendance/Scheduling, actor-level teacher gating with admin bypass, seeded demo assignment. Merge order: #40 → #39 → #41. Follow-up (roadmap): assignment web/API administration and bulk tooling.
2. **Learner portal onboarding + results notifications** — slices (b) and (c) **done and merged (PR #44)**: report-card publication notifications to learner + guardians, and the learner's `My report cards` portal page with nav link. **Remaining: slice (a)** — learner invitation/onboarding service + acceptance flow mirroring `GuardianInvitationService` (token table, queued mail, acceptance controller/views, permissions, tests) — the next task. Optional later portal surfaces: attendance summary and timetable pages.
   **Also shipped en route** (merged via #43): teacher parent reports on study plans, Ollama transport-error wrapping, and the deterministic study-plan fallback. The consolidated docs PR is also merged (#45). **All planned merges are complete.**
3. **Operational trust:** tested backup restore in CI, deployment automation, enforced 2FA.
4. **Hardening:** test depth for Attendance/Reports/Scheduling/Staff; live Claude/Gemini AI adapters; module-registry enable/disable semantics; delete-or-justify stub cores (Billing/Events/FileManagement).
5. **Real billing** behind `core/Billing` (needs a gateway/pricing decision).
6. Deferred: homework/content, online exams, promotion/rankings, staff attendance, mobile/offline, integrations, additional modules.

## Working method (from AGENTS.md — unchanged)

Branch per task off `main`; never implement on `main`; draft PRs via the publishing flow; humans merge (or explicitly authorize a merge). Proportional verification before handoff: targeted tests → `make test`, `make pint`, `make analyse`, `make migrate-check` when migrations change.
